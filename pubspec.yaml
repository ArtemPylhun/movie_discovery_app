name: movie_discovery_app
description: "Flutter mobile application for discovering movies and TV shows. Features include browsing popular movies, search functionality, favorites management, and offline support. Educational project for learning Flutter development with Clean Architecture."
publish_to: "none"
version: 0.1.0

environment:
  sdk: ^3.2.0

dependencies:
  flutter:
    sdk: flutter

  # State Management
  flutter_riverpod: ^2.4.0
  riverpod_annotation: ^2.1.1

  # Network & API
  dio: ^5.3.2
  retrofit: ^4.0.1
  json_annotation: ^4.8.1

  # Local Storage
  path_provider: ^2.1.1
  hive: ^2.2.3
  hive_flutter: ^1.1.0

  # Dependency Injection
  get_it: ^7.6.4
  injectable: ^2.1.2

  # UI & Images
  cached_network_image: ^3.2.3
  shimmer: ^3.0.0
  lottie: ^2.6.0

  # Navigation
  go_router: ^10.1.2

  # Utils
  either_dart: ^1.0.0
  equatable: ^2.0.5
  freezed_annotation: ^2.4.1
  intl: ^0.18.1
  flutter_dotenv: ^5.1.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

  build_runner: ^2.4.7
  freezed: ^2.4.6
  json_serializable: ^6.7.1
  retrofit_generator: ^8.0.4
  riverpod_generator: ^2.3.6
  injectable_generator: ^2.1.6

  # Testing
  mockito: ^5.4.2

flutter:
  uses-material-design: true
